============
Installation
============

At the command line::

    $ easy_install django-pgcli

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pgcli
    $ pip install django-pgcli
